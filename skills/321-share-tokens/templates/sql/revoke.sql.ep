-- Owner-only revoke; idempotent (revoking twice keeps the first timestamp).
UPDATE share_tokens
SET revoked_at = COALESCE(revoked_at, now())
WHERE share_token_id = [share_token_id]
AND created_by_user_id = [created_by_user_id]
RETURNING share_token_id, resource_type, resource_id, revoked_at
