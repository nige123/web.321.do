# conf/secrets.local.conf  -  per-host secrets, NEVER committed (git-ignored).
# Copy to conf/secrets.local.conf on each host and fill in real values. The
# development/production .conf files merge this hashref over themselves.
{
    # postmark_server_token => 'xxxx',   # real transactional-email token
    # Stripe keys (added when you adopt the 321-stripe skill):
    # stripe_secret_key     => 'rk_test_...',   # restricted key, never sk_
    # stripe_webhook_secret => 'whsec_...',
}
